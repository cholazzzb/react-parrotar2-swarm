import * as util from "./Util.js"

function PSOParticle(curretPos){
        this.curret_position = currentPos;
        this.current_velocity = [0,0,0]
        this.personal_best_pos = currentPos
}
